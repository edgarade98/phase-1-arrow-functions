// function expression named divide that divides the two parameters placed in the function
function divide () {
    return 2000 / 100;
}

//arrow function named square that takes one parameter and multiplies by itself
const square = x => x * x;

//arrow function named add that takes two parameters and adds them
const add = (a, b) => a + b;
const result = add(3, 4);
console.log(result);

